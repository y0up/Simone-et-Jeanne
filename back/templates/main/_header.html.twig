<header>
    <h3>Acceuil</h3>
    <a href="{{ path('product_index_new') }}">Nouveaute</a>
    {% for category in categories %}
    <a href="{{ path('product_index_cat', {'id' : category.id}) }}">{{category.name}}</a>
    {% endfor %}
    <h3>A Propos</h3>
    <h3>Contact</h3>
</header>